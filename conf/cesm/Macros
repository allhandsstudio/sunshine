#
# Makefile Macros generated from /cesm-1_2_2/scripts/ccsm_utils/Machines/config_compilers.xml using
# COMPILER=gnu
# OS=Linux
# MACH=userdefined
#
CPPDEFS+= -DFORTRANUNDERSCORE -DNO_R16 -DLinux -DCPRGNU

SLIBS+=-L/usr/lib/ -L/usr -lnetcdff -Wl,-Bsymbolic-functions -Wl,-z,relro -lnetcdf -lnetcdf

CONFIG_ARGS:=

CXX_LINKER:=FORTRAN

ESMF_LIBDIR:=

FC_AUTO_R8:= -fdefault-real-8

FFLAGS:= -O -fno-range-check -fconvert=big-endian -ffree-line-length-none -ffixed-line-length-none

FFLAGS_NOOPT:= -O0

FIXEDFLAGS:=  -ffixed-form

FREEFLAGS:= -ffree-form

MPICC:= mpicc

MPICXX:= mpicxx

MPIFC:= mpif90

MPI_LIB_NAME:=

MPI_PATH:=

NETCDF_PATH:= /usr

PNETCDF_PATH:=

SCC:= gcc

SCXX:= g++

SFC:= gfortran

SUPPORTS_CXX:=TRUE

ifeq ($(compile_threaded), true)
   FFLAGS += -fopenmp
   CFLAGS += -fopenmp
   LDFLAGS += -fopenmp
endif

ifeq ($(DEBUG), TRUE)
   FFLAGS += -g -Wall
endif

ifeq ($(MODEL), pop2)
   CPPDEFS += -D_USE_FLOW_CONTROL
endif

ifeq ($(MODEL), cism)
   CMAKE_OPTS += -D CISM_GNU=ON
endif